import { PhotoGalleryDirective } from './photo-gallery.directive';

describe('PhotoGalleryDirective', () => {
  it('should create an instance', () => {
    const directive = new PhotoGalleryDirective();
    expect(directive).toBeTruthy();
  });
});
