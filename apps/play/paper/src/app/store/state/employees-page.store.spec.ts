import { EmployeesPageStore } from './employees-page.store';

describe('EmployeesPageStore', () => {
  it('should create an instance', () => {
    expect(new EmployeesPageStore()).toBeTruthy();
  });
});
